void cmpr_encoder(streaming chanend cin, streaming chanend cout, int w, int h);
void cmpr_decoder(streaming chanend cin, streaming chanend cout, int w, int h);

void cmpr_encoder_with_bypass(
    streaming chanend cin, 
    streaming chanend bypass, 
    streaming chanend cout, 
    int w, int h);

void cmpr3_encoder(streaming chanend cin, streaming chanend cout, int w, int h);
void cmpr3_decoder(streaming chanend cin, streaming chanend cout, int w, int h);
